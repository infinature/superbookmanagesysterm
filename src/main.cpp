#include "head.h"

using namespace std;

int main() {
    //BuildIndex();
    //std::locale::global(std::locale("C.UTF-8"));
    User a;
    string b;
    lookBook();
    BorrowBook(a,b);
    return 0;
}